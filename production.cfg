[buildout]
extends = base.cfg journal.cfg versions.cfg
extensions =
parts +=
    awscli
    logrotate.conf
    logrotate-hourly
    autostart

[circus.ini]
webapp = True
statsd = False

[logrotate.conf]
logs_bucket = openprocurement-logs

[versions]
